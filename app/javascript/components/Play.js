import React from "react"
import PropTypes from "prop-types"
class Play extends React.Component {
  render () {
    return (
      <React.Fragment>
      </React.Fragment>
    );
  }
}

export default Play
